# The active Spring profile for the inventory-service (e.g., dev, prod)
SPRING_PROFILES_ACTIVE=dev

# The host and container port mapping for the inventory service.
# For dev, these default to 8081. For testing prod locally, use 8082.
INVENTORY_HOST_PORT=8081
INVENTORY_CONTAINER_PORT=8081
